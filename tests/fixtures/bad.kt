package example

// Loads the user — then caches it
fun loadUser() {}

/** Validate; then save. */
fun save() {}

/**
 * First line.
 * Second line.
 * Third line.
 */
fun three() {}

// TODO: handle errors
fun todo() {}

val s = "text with — dash; // not a comment"
