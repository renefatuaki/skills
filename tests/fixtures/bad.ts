export const a = 1

// Loads the user — then caches it
export function loadUser() {}

/** Validate; then save. */
export function save() {}

/**
 * First line.
 * Second line.
 * Third line.
 */
export function three() {}

// TODO: handle errors
export function todo() {}

const s = `text with — dash; // not a comment`
